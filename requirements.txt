
# Trimmed requirements: only direct dependencies used by the application and evaluation
fastapi==0.128.0
uvicorn==0.40.0
SQLAlchemy==2.0.45
pydantic==2.12.5
pydantic-settings==2.12.0
python-dotenv==1.2.1

# Postgres / DB
psycopg==3.3.2
psycopg-pool==3.3.0
pgvector==0.3.6

# Authentication
bcrypt==5.0.0
PyJWT==2.10.1
python-multipart==0.0.21

# Langchain / LLM integrations used in code
langchain-core>=0.3
langchain-experimental
langchain-postgres
langchain-groq
langchain-ollama
langchain-text-splitters

# LangGraph (conversation memory)
langgraph==1.0.7
langgraph-checkpoint-postgres==3.0.3

# LangSmith / evaluation helpers
langsmith==0.6.5

# Optional / helper libraries used directly in code
FlashRank==0.2.10

# Utilities and runtime expectations
requests==2.32.5
orjson==3.11.5
tqdm==4.67.1

# Keep tokenizers/tiktoken if any LLM backends expect them
tiktoken==0.12.0
tokenizers==0.22.2

# Keep minimal typing/runtime helpers
typing_extensions==4.15.0

email-validator==2.3.0

